{
  "name": "@griot/agent",
  "version": "0.0.0",
  "private": true,
  "description": "Griot's reasoning core — provider-agnostic LLM access (Gemini primary, Bedrock behind a flag)",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@griot/db": "*",
    "@slack/web-api": "^7.18.0",
    "pino": "^9.7.0"
  }
}
